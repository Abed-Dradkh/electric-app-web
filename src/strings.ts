/**
 * User-visible copy for house vs bench terminology (extend for i18n later).
 */
export const COPY = {
  paletteAcSupplyHint:
    'House wiring: connect line (L) to neutral (N) through loads.',
  paletteDcSupplyHint:
    'Bench parts: use wires between pins to build a DC circuit.',
  paletteAcPartsHint:
    'Click two pins to run a wire. Hover a part for remove and switch/breaker controls.',
  paletteDcPartsHint:
    'Click two pins to run a wire. Hover a part for remove and switch On/Off.',
} as const;
